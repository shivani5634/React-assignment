import React from "react";
import { Button, Icon } from "rsuite";

const ShoppingCart = () => {
  return (
    <div>
      <Button>
        <Icon icon="cart">bskt</Icon>
      </Button>
    </div>
  );
};

export default ShoppingCart;
